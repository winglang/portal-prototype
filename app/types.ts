export interface Params {
  group: string;
  version: string;
  plural: string;
  namespace: string;
  name: string;
}

export type ApiGroup = {
  group: string
  version: string
  icon: string
  plural: string
}
